import React from 'react';

/**
 * Placeholder clients list page. This will show the list of clients with
 * filters and allow navigation to individual client profiles.
 */
const ClientsListPage: React.FC = () => {
  return <div className="text-lg font-semibold">Clients list placeholder</div>;
};

export default ClientsListPage;
