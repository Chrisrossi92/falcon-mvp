import React from 'react';

/**
 * Placeholder dashboard page for the Falcon MVP. This will eventually display
 * high-level KPIs, task lists, and charts as defined in the design doc.
 */
const DashboardPage: React.FC = () => {
  return <div className="text-lg font-semibold">Dashboard placeholder</div>;
};

export default DashboardPage;
